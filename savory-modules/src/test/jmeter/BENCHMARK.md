# 秒杀压测基准报告

> 运行方式见 `run_load.sh`。前置：后端运行、`gen_tokens.sh` 生成 tokens.csv、重置活动库存。

## 基准记录 #1 — 500 并发（2026-09-03）

### 环境
- 后端：savory-modules（事务消息版，seckill 已升级 RocketMQ 事务消息），localhost:8080
- 中间件：Docker（MySQL 8 / Redis 7 / RocketMQ 5.3.0）
- JMeter：apache-jmeter-5.6.3
- 活动：id=7，dish_id=1，初始库存 1000，限购 1/人

### 压测参数
- 线程数 500、ramp-up 2s、每线程 1 次迭代、500 个独立用户 token（CSV 参数化）

### 结果（HTTP 层，JMeter summary）

| 指标 | 值 |
|---|---|
| 总请求数 | 500 |
| HTTP 成功 | 500（Err: 0.00%） |
| 总耗时 | 2s（≈230/s） |

### 业务层分布（限流保护生效）

| 业务结果 | 数量 | 说明 |
|---|---|---|
| 秒杀成功 (code=1) | 272 | 放行的请求全部建单成功 |
| 被限流拒绝 (code=0「请求过于频繁」) | 228 | `@RateLimit` 200 QPS 全局桶保护 |

### 延迟（500 样本，Latency 列）

| 指标 | 值 |
|---|---|
| 平均 | 230ms |
| p50 | 36ms |
| p90 | 657ms |
| p95 | 737ms |
| p99 | 867ms |
| 最大 | 904ms |

> 解读：p50 仅 36ms（多数请求极快）；p90+ 尾延迟高是因被限流的请求在 Redis 令牌桶竞争等待，属保护机制代价，非系统瓶颈。

### 一致性校验（压测后三方一致）

| 数据源 | 值 | 判定 |
|---|---|---|
| Redis 库存 | 728 | ✓ (1000-272) |
| DB stock | 728 | ✓ |
| DB sold | 272 | ✓ |
| 订单数 | 272 | ✓ |
| 是否超卖 | 否（272 ≤ 1000，Redis=DB=订单） | ✅ |

### 结论
- **500 并发无超卖、无系统错误**，Redis/DB/订单三方严格一致。
- **限流保护按设计工作**：`seckill:buy` 全局限流 200 QPS，瞬时 500 并发时 228 被优雅拒绝（HTTP 200 + 业务 code=0），未压垮后端。
- 放行请求 100% 建单成功，事务消息链路在高并发下稳定。
- 备注：若要测更高真实吞吐（>200 QPS），需调高 `@RateLimit` 的 permitsPerSecond 或改为按用户维度限流。
